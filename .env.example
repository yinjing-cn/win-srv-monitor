# Server
HOST=0.0.0.0
PORT=5000

# Mock mode (set to 1 to use fake data on non-Windows)
MOCK_MODE=1

# Collection interval (seconds)
COLLECT_INTERVAL=30

# Data storage
DATA_DIR=data
HISTORY_FILE=data/history.json

# Alert thresholds
CPU_THRESHOLD=80
MEMORY_THRESHOLD=85
DISK_THRESHOLD=90
